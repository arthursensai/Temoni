const QuoteBox = () => {
  return (
    <p className="text-2xl w-3/4 text-center font-bold">
      &ldquo;Love is not a promise spoken, but a presence that proves itself in
      absence.&ldquo;
    </p>
  );
};

export default QuoteBox;
